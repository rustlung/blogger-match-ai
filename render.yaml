services:
  - type: web
    name: blogger-match-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.web:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
